{
  "name": "astroparty",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "frontend:start": "yarn workspace @astroparty/frontend dev",
    "frontend:build": "yarn workspace @astroparty/frontend build",
    "multiplayer:start": "yarn workspace @astroparty/multiplayer start",
    "shared:build": "yarn workspace @astroparty/shared build",
    "engine:build": "yarn workspace @astroparty/engine build",
    "lint": "yarn lerna run lint",
    "lint:fix": "yarn lerna run lint:fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "husky": "^8.0.3",
    "lerna": "^5.1.4",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "@prisma/client": "^4.2.1"
  },
  "workspaces": [
    "packages/*"
  ]
}