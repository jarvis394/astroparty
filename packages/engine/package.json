{
  "name": "@astroparty/engine",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@colyseus/schema": "^2.0.11",
    "events": "^3.3.0",
    "mainloop.js": "^1.0.4",
    "matter-attractors": "^0.1.6",
    "matter-js": "^0.19.0",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/events": "^3.0.0",
    "eslint": "^8.46.0",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0",
    "tsc-alias": "^1.8.7"
  },
  "scripts": {
    "build": "yarn clean && tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo"
  }
}
