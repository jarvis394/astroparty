{
  "name": "@astroparty/shared",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "class-validator": "^0.13.2",
    "typescript": "^5.1.6",
    "colyseus": "^0.15.7"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  },
  "scripts": {
    "build": "yarn clean && tsc -p tsconfig.json",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo"
  }
}