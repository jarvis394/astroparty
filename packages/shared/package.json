{
  "name": "@astroparty/shared",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@astroparty/engine": "^1.0.0",
    "@geckos.io/snapshot-interpolation": "^1.1.0",
    "class-validator": "^0.13.2",
    "colyseus": "^0.15.7",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  },
  "scripts": {
    "build": "yarn clean && tsc -p tsconfig.json",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo"
  }
}
